# 光学解谜 · 框架搭建说明

> 本文档描述在本仓库（`LightPuzzle`）内搭建「光学解谜」玩法骨架时的**推荐工程结构、分层与初始化顺序**，与 `小游戏设计文档.md`（v0.7+）及 `.cursor/rules/project-rules.mdc` 对齐。  
> 当前工程 `package.json` 中 **Cocos Creator 版本：3.8.4**（升级引擎时需同步修订本说明）。

---

## 1. 目标与边界

- **目标**：可运行的最小闭环（加载关卡 → 操作 → 光路重算 → 胜负 → 下一关/重置），模块边界清晰，便于后续加关卡与表现。  
- **边界**：Core 不写 `Node` / `Prefab` 引用；Presentation 不写胜负判定核心规则；跨模块用**事件**或**显式接口**，避免隐式全局耦合。

---

## 2. 推荐目录结构

在 `assets` 下为玩法单独划目录（与项目规则中的「新游戏模板」一致，名称可按团队习惯微调）：

```
assets/
├── Scripts/
│   └── Games/
│       └── OpticalPuzzle/          # 或 光学解谜 / LightPuzzleGame 等统一命名
│           ├── Core/               # 纯规则：网格、推动合法性、光追、胜负
│           ├── Application/        # 关卡加载、历史栈、撤回(5步)、重置、状态机
│           ├── Presentation/       # 格子视图、光束、四向按钮、HUD
│           └── Config/             # 关卡 Schema、默认常量（可选）
├── Prefab/                         # 局内 UI、格子单元等预制体
└── Scenes/
    ├── Menu.scene                  # 若保留独立菜单
    └── GameOptical.scene           # 或沿用现有 Game 场景名，以团队为准
```

**说明**：上图仅展开 **`Games/OpticalPuzzle/`** 玩法子树。`assets/Scripts/` **根级** 的 `Manager/`、`Utils/`（含 `Event.ts`、`Enum.ts` 等）仍按 **`project-rules.mdc`** 全文维护；§4「与现有工程规范的衔接」即指向该部分，而非被本图省略。若短期不拆 `Games/` 子目录，也至少保证 **Core 与 Component 分离**，避免规则脚本挂在大量 UI 节点上。

---

## 3. 分层职责（与设计文档 §6 对应）

| 层级 | 职责 | 禁止事项（原则） |
|------|------|------------------|
| **Core** | 地形与元件快照、主角坐标、推动一步的合法性与状态迁移、光线模拟（含分光分支与环检测）、是否通关 | 不持有场景节点、不播放音频 |
| **Application** | 读取关卡数据、构建 Core 初始状态、维护操作历史、撤回 5 步、重置本关、驱动 `BOOT`→`READY`→`RUNNING`→`SETTLEMENT` 等 | 不写具体绘制逻辑 |
| **Presentation** | 四向按钮、撤回/重置、选关 UI；将输入转为「方向指令」；根据 Core 快照刷新格子与光束；**主角与元件的移动/推动挤压拉伸**、**表情与待机状态机**、**可选粒子**（与 Core 已确认的一步结果同步；见《小游戏设计文档》§4.1、§7.4） | 不单独决定「是否胜利」（由 Application 根据 Core 结果更新状态） |

---

## 4. 与现有工程规范的衔接

以下内容与 `project-rules.mdc` 一致，搭建时**建议直接复用或对齐**：

1. **Manager 模式**：局内流程（如 `GameManager` / `LevelManager`）仅做编排，规则委托给 Core。  
2. **事件系统**：音效、Toast 等通过 `Event.ts` 中定义的事件触发，监听在 `onLoad` 注册、`onDestroy` 注销。  
3. **DataManager**：关卡解锁进度、音量、可选设置等持久化；键名与合并默认值与现有项目约定一致。  
4. **MusicManager**：持久化根节点；BGM / 音效通过事件播放，Core 不直接拿 `AudioSource`。  
5. **通用 UI**：Toast、RoundBox 等按需挂到局内 HUD。

---

## 5. 关卡数据与兼容

- **来源**：JSON / TS 导出表，字段见 `小游戏设计文档.md` §5（地形层、`type`、`colorMode`、光源与目标、portal 组 id 等）。  
- **加载管线**：解析 → 校验（portal 成对、尺寸合法）→ 构建 Core 初始状态 → 通知 Presentation 全量刷新。  
- **兼容**：旧 `filter-*` → `glass` + `colorMode`；旧 `stone` → `wall`（地形层）。

---

## 6. 场景与节点建议

| 节点/模块 | 用途 |
|-----------|------|
| 棋盘容器 | 动态实例化格子或对象池复用单元格 |
| HUD 层 | 四向按钮、撤回、重置、关卡号、（可选）选关 |
| 光束层 | `Graphics` 或自定义绘制，置于棋盘之上 |
| 持久化 | `MusicManager` 等沿用项目既有做法 |

---

## 7. 初始化顺序（建议）

1. `DataManager`（或等价）恢复存档与设置。  
2. 进入局内场景 → Application 选择关卡 id → 加载配置。  
3. Core `reset(levelSnapshot)`。  
4. Presentation 绑定按钮事件，首次全量渲染。  
5. 状态置为 `READY` / `RUNNING`（是否拆分看是否需要开局动画）。

---

## 8. 验收清单（框架阶段）

- [ ] 能从配置加载至少 1 关并显示网格与占位美术。  
- [ ] 四向指令进入 Application → Core 更新 → UI 刷新链路打通。  
- [ ] 重置本关与撤回 5 步与历史栈一致，无事件泄漏。  
- [ ] Core 可在不启动场景的情况下被单元测试调用（可选但推荐）。

---

## 9. 编辑器挂接（代码已落地后）

1. 打开 `Menu.scene`（或新建局内场景），在 **Canvas** 下建空节点 `OpticalPuzzle`。  
2. 在同一节点或子节点上添加组件：**`OpticalPuzzleRoot`**、**`OpticalPuzzleBoardView`**、**`OpticalPuzzleInputHud`**（Root 会自动 `getComponentInChildren` 查找后两者）。  
3. 在场景中单独节点挂 **`MusicManager`**，绑定 `AudioSource`、可选 `clickButton`（可先留空，逻辑侧已做空判断）。  
4. 预览：未绑四向 **Button** 时可用 **方向键 / WASD** 移动、`Z` 撤回 5 步、`R` 重置；绑定按钮后在属性检查器拖入 `btnUp` 等。  
5. 若需批量重建 `assets/Scripts` 下 `.meta`：`powershell -ExecutionPolicy Bypass -File tools/gen-optical-meta.ps1`，再在 Cocos 中刷新资源。

---

## 10. 修订记录

| 版本 | 说明 |
|------|------|
| v0.1 | 初稿：与设计文档 v0.7、Cocos 3.8.4、project-rules 对齐 |
| v0.2 | Presentation 职责补充：挤压拉伸、主角表情状态机、粒子；对齐设计文档 v1.0 |
| v0.3 | §2 补充：目录树仅为 Games 子树，Scripts 根级仍按 project-rules |
| v0.4 | §9～§10：Manager/Utils/Presentation 代码落地后的编辑器挂接与 meta 脚本说明 |

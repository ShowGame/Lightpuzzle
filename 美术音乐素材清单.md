# 光学解谜 · 美术与音乐素材清单

> 与 `小游戏设计文档.md`（v1.0+）一致：**挡光合并为 `wall`**（石块外观可用墙地块变体）；光学元件为 **8 类几何 `type` + 统一 `colorMode`**（滤光可由程序染色弱化贴图数量）。  
> 美术表现以**团队既有像素风**为基底；Patrick's Parabox 仅作可读性/构图参照（见 §0）。  
> 下列数量为**最小可发行（MVP + Vertical Slice）**建议；实际以主美定稿为准。

---

## 0. 画风参照（帕特里克的箱子）

**表现基底**：本项目**沿用团队既有「像素风」**（分辨率、调色板与绘制流程以既有工程为准），不在此改为写实或 3D 渲染管线。

**参照对象**：独立解谜游戏 **Patrick's Parabox**（中文常称「帕特里克的魔盒」；团队可按习惯称「帕特里克的箱子」）。参照范围仅限于**构图与可读性气质**——例如块面分明、格子与箱子一眼可辨、色彩对比清楚、UI 大块好点；**不是**换皮目标，也**不会**直接使用其贴图、模型或商标。

| 维度 | 建议（在**像素风**前提下学气质） |
|------|-------------------------------------|
| **造型** | 体块略「敦实」、轮廓像素级清晰；光学元件剪影可辨，避免过碎的高频细节。 |
| **色彩** | 饱和适中、对比明确；地面与墙体色阶分开；光路与高亮控制在少量主色内。 |
| **透视** | 像素项目多采用 **正交俯视 / 轻微斜俯视** 即可；若做伪等轴，须与像素网格对齐，全关统一。 |
| **描边与明暗** | 可用清晰轮廓色或有限层数阴影，减少写实纹理噪声。 |
| **UI** | 按钮大块、留白足，与格子块面语言一致。 |
| **动效气质** | 主角移动、推动成功时的 **短时挤压拉伸** 可参考 Parabox 的「弹性」反馈；在像素风下用 **整数像素位移 / 受控 scale 曲线** 实现，避免糊边。 |

**创作约定**：所有精灵、UI、音频均为**自有或已授权资产**；Patrick's Parabox 仅作内部**画风与版式**讨论时的参照，**不构成素材来源**（团队已明确不会照抄原作资源）。

---

## 1. 美术素材 · 精灵 / 图集

### 1.1 地形（地块）

| 资源 id / 用途 | 说明 | 数量（约） |
|----------------|------|------------|
| `floor` | 可走地板 | 1 |
| `wall` | 不可走、挡光；**石块外观**可做 1 张变体或同一套色阶 | 1～2 |
| `source` | 光源底座（箭头或光晕可代码画） | 1 |
| `target` | 接收器底座（点亮态可代码改色） | 1 |

### 1.2 主角（正方形 + 眼睛 + 动画）

主角为 **占一格的正方形**，外轮廓线宽与 **光学元件方块** 一致，便于对齐与读题。

| 资源 / 动画 | 说明 | 数量（约） |
|-------------|------|------------|
| 身体基底 | 正方形像素精灵（可分层：身体 + 眼白 + 眼珠） | 1 套基底 |
| 眼部四向 | 移动方向 **上/下/左/右** 时眼睛相对主体的 **位置或贴图** 变化（四向可读） | 4 组或 1 组+节点偏移方案 |
| 短待机循环 | 眨眼、微挤压等轻表情 | 1 套循环（若干帧） |
| 长待机 | 无操作超时后的「无聊 / 犯困 / 张望」等 | 1～2 套循环或触发式片段 |
| 移动中表情 | 与 **走一步** 的形变关键帧对齐 | 可与四向共用或每向加帧 |
| 推动中表情 | 与 **推动光学元件一步** 对齐，可与移动区分 | 可选独立 4 向或复用移动+叠加 |

> 帧数与图集打包以实现规格为准；可先 **占位 + 程序挤压拉伸**，再迭代像素帧动画。

### 1.3 光学元件（几何基底）

每种 `type` 至少 1 张底图；**portal** 建议 A/B 各 1 张以便区分。

| `type` | 说明 | 数量（约） |
|--------|------|------------|
| `glass` | 全向透光石 | 1 |
| `glass-h` / `glass-v` | 定向透光 | 各 1 |
| `mirror-slash` / `mirror-backslash` | 斜杠 / 反斜杠镜 | 各 1 |
| `splitter-t` / `splitter-cross` | 分光棱镜 | 各 1 |
| `portal-*a` / `portal-*b` | 传送门成对 | 2 |

**`colorMode`（滤光）**：可与基底共用贴图，靠**叠加色 / 小图标角标**区分 `through` / `filterRed` / `filterGreen` / `filterBlue`，减少「每种元件 ×4 色」的贴图爆炸。

**推动形变（可选）**：光学元件被推动时的挤压拉伸可与主角 **共用一套时间曲线**，幅度略小；亦可仅程序 scale、不增加贴图。

### 1.4 UI

| 用途 | 说明 |
|------|------|
| 四向按钮 | 上/下/左/右，常态 + 按下态（可选） |
| 撤回 / 重置 | 图标 + 可选文字 |
| 关卡标题 / 胜利失败面板 | 与项目 UI 风格统一 |
| 选关列表（可选） | 列表项背景、滚动条 |

### 1.5 代码驱动与粒子（可不出图 / 少贴图）

- 光束路径、光晕、光源箭头方向。  
- 目标点亮前后对比（颜色、描边、透明度）。  
- **挤压拉伸**：主角与可推动元件可用 **Tween 非均匀 scale** 或序列帧实现 §0 动效气质；与 `小游戏设计文档` §4.1 对齐。  
- **粒子（增强表现）**：落脚尘、推动摩擦星点、点亮细屑、胜利撒点等，使用 **小尺寸、短寿命、低发射率** 的像素风粒子（Cocos `ParticleSystem2D` 等）；须设 **同屏上限**，避免性能与读题干扰。  
- 胜利整体缩放或全屏淡入淡出可与粒子二选一或组合。

---

## 2. 音乐素材

| 类型 | 用途 | 数量（约） |
|------|------|------------|
| BGM | 菜单 / 局内 | 2 |

**规范**：由 `MusicManager` 播放；场景切换与暂停策略与 `project-rules.mdc` 一致。

---

## 3. 音效素材

移动 / 推动类音效可在实现上与挤压拉伸的 **峰值或回弹** 对齐，增强「重量感」（与 §1.5 动效配合）。

| 事件 | 说明 | 数量（约） |
|------|------|------------|
| UI 点击 | 通用按钮 | 1 |
| 移动成功 | 主角走一步 | 1 |
| 推动成功 | 推动光学元件 | 1 |
| 移动/推动失败（可选） | 撞墙或非法操作 | 1 |
| 接收器点亮 | 单目标达成 | 1 |
| 关卡胜利 | 结算前反馈 | 1 |

**规范**：通过 `PLAY_AUDIO` 等事件触发，不在 Core 内直接 `playOneShot`。

---

## 4. 命名与导出建议

- 纹理：**PNG**，主角与需叠加的元件带透明通道。  
- 统一放在 `assets` 下资源目录（如 `assets/Textures/OpticalPuzzle/`），图集按 UI / 地块 / 元件分包。  
- 音频：**OGG** 或项目已采用格式；音量在 `DataManager` / 设置里可调。

---

## 5. 修订记录

| 版本 | 说明 |
|------|------|
| v0.1 | 初稿：与设计文档 v0.7 元件与操作对齐 |
| v0.2 | 增加画风参照 Patrick's Parabox（帕特里克的箱子/魔盒） |
| v0.3 | 明确：表现基底为团队既有像素风；Parabox 仅构图/可读性参照；不构成素材来源 |
| v0.4 | 主角正方形+眼睛四向与待机/长待机/移动推动表情；挤压拉伸与粒子；光学元件推动形变；对齐设计文档 v1.0 |

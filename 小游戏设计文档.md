# 小游戏设计文档 · 光学解谜

> 文档版本：v1.0（草案，后续与策划、程序、美术对齐后修订）  
> 引擎与工程：Cocos Creator（本仓库 `LightPuzzle`）  
> 架构取向：参考 `.cursor/rules/project-rules.mdc` — Manager + 事件驱动 + 配置与逻辑分层。

---

## 1. 游戏概述

| 项目 | 说明 |
|------|------|
| 游戏名称 | **光学解谜** |
| 游戏类型 | 2D 网格 **推箱子** + **光学解谜** |
| 核心体验 | 在有限空间内推动光学元件，调整光路，使光线以正确颜色到达全部接收器 |
| 操作方式 | **屏幕四向按钮**：上 / 下 / 左 / 右，**点击**对应方向移动（或推动）|
| 单局目标 | 所有接收器按规则被点亮 → 关卡胜利 |

玩家控制主角在可行走格子上移动，按经典推箱子规则推动各类光学元件，改变或分路光束，满足颜色与路径约束。

---

## 2. 设计目标与原则

1. **规则先于表现**：核心光路、阻挡、染色、分光、传送等逻辑可在无华丽特效下验证（便于单测与关卡迭代）。  
2. **配置驱动关卡**：地形、光源、元件初始布局、胜利条件尽量数据化，避免硬编码。  
3. **模块边界清晰**：  
   - **Core**：纯网格规则、光线传播、胜负判定（不依赖 `Node` / `Prefab`）。  
   - **Application**：开局、重置、**操作历史栈**（供撤回按钮每次弹出最多 5 步）、关卡切换、状态机（`BOOT` → `READY` → `RUNNING` → …）。  
   - **Presentation**：格子渲染、光束表现、输入映射、音效与 UI。  
4. **与工程规范一致**：跨模块通信优先事件；全局进度等通过 `DataManager` 或等价单例；`onDestroy` 必须清理监听与定时器。

---

## 3. 核心规则

### 3.1 主角移动

- 主角 **仅可进入**：`floor`（普通地板）。  
- 主角 **不可进入**：`wall`、`source`、`target`、已被**可推动**光学元件占据的格子。  
- **推动规则**（仅适用于 §3.3 **光学类**元件）：仅可 **推** 不可拉；一次只推一个；目标格后方须可接收（空或规则允许）。**`wall` 不可推动**。

### 3.2 地形系统

| 地形 | 主角可进入 | 光线可通过 | 说明 |
|------|------------|------------|------|
| `floor` | ✅ | ✅（无遮挡时） | 基础地面 |
| `wall` | ❌ | ❌ | 完全阻挡；不可推动。若需「石块」等外观，使用 `wall` 配不同地块/精灵即可，**不再单独定义 `stone` 类型** |
| `source` | ❌ | — | 发射源，位置固定；颜色可配置，默认白光 |
| `target` | ❌ | — | 光到达且颜色匹配则点亮；**全部目标满足则通关** |

### 3.3 基础元件类型（光学类，仅此若干）

关卡中**可推动的光学元件**基础类型只保留下表；几何与光学行为由 `type` 决定，**颜色层面的行为**不由单独类型名区分，而由 §3.4 的**颜色属性**统一表达。

| `type` | 光学特性（摘要） | 备注 |
|--------|------------------|------|
| `glass` | 任意方向直线穿过 | 旧策划中的 **`filter-red` / `filter-green` / `filter-blue`** 均视为本类型的**有色情况**（见 §3.4），不再作为独立 `type` |
| `glass-h` | 仅水平方向穿过；垂直挡光 | `dir` 与网格轴向在实现时统一枚举 |
| `glass-v` | 仅垂直方向穿过；水平挡光 | 同上 |
| `mirror-slash` / `mirror-backslash` | 斜面反光；反射分别为逆时针 / 顺时针 90°（与策划表一致） | **不可旋转**；由关卡摆放与推动改变位置 |
| `splitter-t` | T 型分光：除入射反向外三路输出 | 分支光追 + **环检测** |
| `splitter-cross` | 十字四分光 | 同上 |
| `portal-*a` / `portal-*b` | 成对传送，出射保持原方向 | `*` 为组 id（如 `portal-0a` / `portal-0b`）；对端缺失或阻挡时行为在配置中写明 |

**光学共性（与策划一致）：**

- **光路模型**：关卡内光线按**几何射线**在格子上传播，仅受墙体、光学元件与主角等**遮挡关系**约束；**不**引入「需介质才能传播」或「暗格 + 覆盖物导光」等与真空/均匀介质中光传播直觉不一致的设定。  
1. 光从 `source` 沿方向直线传播；颜色继承配置（白 / 红 / 绿 / 蓝）。  
2. `glass-h` / `glass-v` 按入射方向判定透射或阻挡。  
3. 遇 `wall`、主角等阻挡体时停止（与 `glass` 族、分光、传送等的交互以类型规则为准）。  
4. 光线与某格光学元件发生交互时：**先**按该格 `type` 处理几何（穿、反、分路、传送），**再**按该格 §3.4 **颜色属性**更新光束颜色或判定阻挡。  
5. 到达 `target`：颜色匹配则点亮。  
6. 分光与多路径：使用栈或队列管理光束分支，并做 **循环检测**。  
7. 传送：从 A 入则从 B 同向继续；边界情况在关卡规范表中逐条列出。

### 3.4 元件颜色属性（透传 / 滤光）

凡 §3.3 表中**光学类**基础元件（`glass`、`glass-h`、`glass-v`、`mirror-slash`、`mirror-backslash`、`splitter-t`、`splitter-cross`、`portal-*a` / `portal-*b`），在关卡数据中均携带同一套 **颜色属性**（字段名实现时自定，如 `colorMode`），至少包含：

| 取值（示例） | 含义 |
|--------------|------|
| `through`（或 `neutral`） | **保持原色**：光经该格几何规则处理后，**颜色与入射一致**（分光则各路均为入射色）。 |
| `filterRed` / `filterGreen` / `filterBlue` | **滤光 / 染色**（与旧 **`filter-red` / `filter-green` / `filter-blue`** 规则一致）：白光经处理后变为对应单色；**已与该色相同时**可直线通过；**其它色**被该格**阻挡**（与旧策划表对齐；若某 `type` 与滤光组合产生歧义，在实现规格中单列真值表）。 |

**与旧命名的关系：** 原 **`filter-red`（红色滤波）** = **`type: glass` + `colorMode: filterRed`**（绿、蓝同理）。其它基础类型在 `colorMode` 非 `through` 时，表示在**该类型几何行为之上**叠加同一套滤光语义（是否 v1 全开由排期决定，文档允许配置存在）。

---

## 4. 操作与交互

| 输入 / UI | 功能 |
|------|------|
| **四向移动按钮**（上、下、左、右） | 点击后向对应方向移动主角；若该方向前方为 **可推动光学元件** 且后方格合法，则执行推动（`wall` 不可推） |
| **撤回按钮** | 点击一次 **连续撤回 5 步**（以「一步」为一次有效移动或推动所引起的状态变更；不足 5 步时撤到开局或栈空为止，具体边界在实现规格中写明） |
| **重置按钮** | 将 **当前关卡** 恢复为初始布局与初始状态（清空本关内的操作历史或与撤回栈一致重置） |
| **选关**（可选） | 跳转已解锁或调试关卡列表（发布版需与进度存档一致） |

**说明：** 默认不提供键盘 WASD / 方向键控制主角；若后续需要无障碍或 PC 快捷操作，可作为可选增强，与四向按钮逻辑共用同一套「发方向指令」接口。

反光镜仅有 **`mirror-slash`** 与 **`mirror-backslash`** 两种，**游戏中不可旋转**，靠关卡配置与推动 reposition 解题。

### 4.1 操作反馈（表现层）

主角**成功移动**或**成功推动光学元件**时，表现层可增加短时 **挤压拉伸（squash & stretch）** 等形变反馈，**气质参照** Patrick's Parabox 的移动「弹性」；实现须与**像素网格/像素风**一致（避免非整数缩放糊边、控制时长与幅度）。被推动的光学元件可与主角使用**同一套动效语言**，幅度可略小于主角以免抢戏。可选叠加**少量粒子**（见 §7.4）。**规则判定仍以 Core 为准**，动效仅跟随已确认的一步结果。

---

## 5. 关卡与内容

- **关卡源数据（四层网格，实施见 `OpticalPuzzleLevelSchema.ts`）**：同尺寸四张 `height × width` 字符表，解析为运行时 `IOpticalLevelConfig`。  
  - **层 1 `staticLayout`**：仅静态占格；**墙与石块统一 `#`**，可走地为 `.`。  
  - **层 2 `objects`**：玩家 `@`、光源 **`S`**、目标 **`E`**、可推动元件几何类型（`o` glass、`-` / `|` 透镜、`/` `\` 镜、`T` / `+` 分光、`{` `}` 传送门等）；墙格 `#`、空地 `.`。  
  - **层 3 `colors`**：**RGBW**（`W`/`R`/`G`/`B`），**仅**在层 2 为 `S`/`E`/元件处填写；墙 `#`、空地/玩家 `.`；该格 `.` 表示默认 `W`。  
  - **层 4 `directions`**：`.` 默认；`w` `a` `s` `d` 表上/左/下/右（光源出射、T 镜、横竖透镜朝向等）；墙 `#`、空地 `.`。  
- **运行时模型**（不变）：`terrain`、`sources`、`targets`、`pieces`（`type` §3.3 + `colorMode` §3.4 + 坐标 + portal 组 id）。类型、颜色、朝向由四层合并写入，**不再**用「一种几何×一种颜色占一个字母」的单表编码。  
- **难度曲线**：教学关 → 单色滤波 → 分光 → 传送 → 综合；每引入一种元件配 1～2 关纯机制关。  
- **胜利条件**：全局 `targets` 均满足；可选扩展：最少步数、星级（后续版本）。

---

## 6. 技术实现要点（Cocos）

| 模块 | 建议职责 |
|------|----------|
| Core | 网格状态、**可推动光学元件**的推动合法性、光线模拟（含分支与环检测）、胜负判定纯函数或可单测类 |
| Application | 关卡加载、操作历史、撤回（每次 5 步）、重置本关、`RUNNING` / `PAUSED` / `SETTLEMENT` 等状态 |
| Presentation | TileMap 或预制格子节点、光束线（`Graphics` / 材质）、高亮、胜利表现；**主角与元件的移动/推动挤压拉伸**、**表情与待机状态机**、**可选粒子**（Presentation 内驱动，与 Core 一步结果同步） |
| 数据 | `DataManager`：关卡进度、设置、可选最少步记录；遵循现有 `save` / `restore` 模式 |
| 音频 | `MusicManager` + `PLAY_AUDIO` / `PLAY_BGM` 事件，不在 Core 内直接播放 |
| 通用 UI | 可复用工程内 Toast、RoundBox 等（见项目规则「跨项目可复用资产」） |

**性能**：单关网格规模有限时，光追每帧全量重算通常可接受；若需优化可做「仅状态变化时重算」。

---

## 7. 美术与表现（最小可行）

### 7.1 静态资源（数量供排期参考，可随表现方案调整）

| 分类 | 数量（约） | 说明 |
|------|------------|------|
| 主角 | 见 §7.4 | 正方形轮廓与元件外轮廓一致；眼睛四向偏移 + 多段表情动画（资源量见《美术音乐素材清单》） |
| 地形 | 4 | floor、wall、source 底座、target 底座 |
| 光学元件 | 与几何类型对应 + 程序染色 | 基底：`glass` / `glass-h` / `glass-v`、双镜、分光、传送对；`colorMode` 用材质或叠加色区分 |

### 7.2 代码驱动效果（减少贴图依赖）

- 光束路径、箭头方向、目标点亮前后色差 / 透明度。  
- **挤压拉伸**：可对主角根节点或元件节点做短时 **非均匀缩放（scale）** 或序列帧变形，与 §4.1 一致。  
- 简单粒子或缩放反馈（胜利、点亮、移动/推动落脚等，见 §7.4）。  
- UI 图标优先使用引擎内矢量/图集或现有项目 UI 组件，避免与 Web 技术栈混用。

### 7.3 风格建议

**表现基底**：**沿用团队既有像素风**（规格与调色以既有工程为准）。

**画风参照**：**Patrick's Parabox**（「帕特里克的魔盒」/ 口语「帕特里克的箱子」）仅作**块面、格子可读性、色彩对比、UI 大块布局及移动时的弹性反馈气质**上的内部参照；**不**更换为原作美术管线，**不**使用原作素材。细则见《美术音乐素材清单.md》§0。

单格分辨率与项目统一（如 64×64）；光学与 `colorMode` 染色在统一色板内做对比；元件贴图基底偏中性，便于运行时叠色。

### 7.4 主角与移动推动表现

1. **主角外形**：占一格的 **正方形**，外轮廓与 **光学元件方块** 在视觉上 **尺度与线宽一致**，便于玩家建立「格子—体块」预期。  
2. **眼睛与朝向**：主角带 **眼睛**；向 **上 / 下 / 左 / 右** 移动时，**眼睛相对正方形主体的位置或朝向发生变化**，用于区分运动方向（可用 4 套眼部贴图、局部节点偏移或少量序列帧）。  
3. **待机动画**：静止时循环 **轻表情动画**（如眨眼、微微挤压）。  
4. **长待机**：超过阈值时间无操作后，播放 **另一套「无聊 / 犯困 / 张望」等** 动画循环或间歇触发（阈值、段数由实现规格表给出）。  
5. **运动与推动**：在 **移动一步**、**推动一步** 的过程中，配合 **表情变化**（可与挤压拉伸关键帧对齐）。  
6. **光学元件**：推动开始或到位时可配合 **短促形变**（与 §4.1 一致），与主角动效语言统一。  
7. **粒子（可选增强）**：如落脚尘、推动时微屑、接收器点亮细屑等，须 **像素风格友好**、**粒子数与寿命有上限**，避免遮挡读题。

---

## 8. 版本规划（草案）

### 8.1 第一阶段（MVP · Prototype）

- [ ] Core：网格 + 主角移动 + 单束光 + wall / glass + 单 target  
- [ ] 一关可玩闭环：胜利判定、下一关或重开  
- [ ] 最小 Presentation：格块 + 简单光线折线 + **四向按钮**发移动指令  

### 8.2 第二阶段（Vertical Slice）

- [ ] 全策划 **基础 `type` + `colorMode`** 规则表（含旧 filter 映射为 `glass`+滤光）  
- [ ] 分光 + 环检测 + 传送 + 滤光模式  
- [ ] 四向按钮移动、撤回按钮（每次 5 步）、重置本关；反光镜不旋转  
- [ ] 音效 / BGM 接入事件系统  
- [ ] 连续若干关「接近上线」体验（含 HUD、失败/胜利反馈）  
- [ ] 主角正方形轮廓、眼睛四向与待机/长待机/移动推动表情；移动与推动的 **挤压拉伸** 与可选 **粒子**（与《美术音乐素材清单》对齐）  

### 8.3 第三阶段（Production）

- [ ] 批量关卡与难度曲线  
- [ ] 本地存档与关卡解锁  
- [ ] 关卡选择 / 图鉴（可选）  

### 8.4 第四阶段（Polish）

- [ ] 最少步数 / 星级、完成动画  
- [ ] 移动端布局与安全区适配（主交互已为按钮点击，与触控一致）  
- [ ] 性能与包体、异常保护、埋点（如需）  
- [ ] 编辑器或导出工具（可选）  

---

## 9. 开放问题（待策划与程序对表）

1. 多光源、多主角是否纳入 v1（当前策划偏单光源 + 单主角）。  
2. 传送门与分光同时存在时的优先级与最大分支数上限。  
3. 接收器是否允许「仅需被照亮」而忽略颜色（教学关）。  
4. 非 `glass` 类型与 `filter*` 颜色模式组合时的完整真值表（尤其分光、传送、多色入射）。  
5. 关卡编辑器归属阶段（Pro 功能 vs 内部工具）。

---

## 10. 文档修订记录

| 日期 | 版本 | 说明 |
|------|------|------|
| （填日期） | v0.1 | 初稿：由策划书整理并适配 Cocos 工程与分层规范 |
| （填日期） | v0.2 | 操作改为四向按钮点击移动；撤回按钮每次回退 5 步；重置本关；反光镜两种且不可旋转 |
| （填日期） | v0.3 | 移除暗格 `dark`；光路为几何射线模型，不采用需介质/暗格覆盖导光 |
| （填日期） | v0.4 | 明确 `filter-*` 与全向透光关系；增加 §3.4 基底无色与有色叠加扩展约定 |
| （填日期） | v0.5 | 光学基础 `type` 收敛为八类 + `stone`；统一 `colorMode`；旧 `filter-*` 映射为 `glass`+滤光 |
| （填日期） | v0.6 | `stone` 固定不可推动；与 `wall` 挡光/占格规则对齐，区分地形层与物件层 |
| （填日期） | v0.7 | 移除 `stone`，与 `wall` 合并；石块外观用地块/精灵表现；旧数据 `stone` → `wall` |
| （填日期） | v0.8 | 美术画风参照 Patrick's Parabox（帕特里克的箱子/魔盒）；详见《美术音乐素材清单》§0 |
| （填日期） | v0.9 | 明确表现基底为既有像素风；Parabox 仅可读性/构图参照，非素材来源 |
| （填日期） | v1.0 | §4.1 移动/推动挤压拉伸；§7.4 主角正方形、眼睛四向、待机与长待机、运动表情、粒子与元件形变 |
| 2026-05-24 | v1.1 | §5 关卡源数据改为四层网格（static / objects / colors / directions）；墙与石块统一 `#`，`S`/`E` 固定于 objects 层 |

---

*本文档为设计与协作基线，实施以代码与关卡数据为准；与 `project-rules.mdc` 冲突时，以团队当期约定为准。*

# LightPuzzle · 光学解谜 · 开发进度

> **用途**：跨轮对话的单一事实来源。每轮 Agent 开发前请先读本文件；每轮结束请更新「最后更新」与「本轮变更」。
>
> **最后更新**：2026-05-25（通道光追 / 混色 / 推箱 / target 规则）  
> **当前阶段**：Prototype（通道 0～4 光追 + 三步混色 + 推箱已可玩；传送 / 选关 UI 待做）  
> **策划/排期依据**：`小游戏设计文档.md`、`项目开发排期.md`、`框架搭建说明.md`、`美术音乐素材清单.md`  
> **代码规范**：`.cursor/rules/project-rules.mdc`

---

## 1. 项目一句话

Cocos Creator 3.8.4 工程 **LightPuzzle**：2D 网格 **推箱子 + 光学解谜**；架构为 Core / Application / Presentation 分层 + Manager + 事件驱动。

---

## 2. 总体进度（对照排期）

| 模块 | 状态 | 说明 |
|------|------|------|
| 目录与分层骨架 | ✅ 已有 | `assets/Scripts/Games/OpticalPuzzle/{Core,Application,Presentation,Config}` |
| 局内入口与占位 UI | ✅ 可跑 | `OpticalPuzzleRoot` + `BoardView`（Graphics）+ `InputHud`；首帧绘制已修 |
| Session / 撤回 5 步 / 重置 | ✅ 可用 | 历史栈含 `player` + `pieces` 坐标；推动/撤回后重算光路 |
| 关卡 **四层网格** 数据与解析 | ✅ 已定 | `parseLayeredGridsToLevelConfig`；层 2 推荐 `0`～`4` 通道；旧 `/` `\|` `T` `+` 兼容 |
| **Menu 场景节点框架** | ✅ 已搭 | `MenuRoot` 三层 UI + `PersistRoot`（Audio/Toast） |
| **Game 场景节点框架** | ✅ 已搭 | `GameRoot` 四层 + `OpticalPuzzle/BoardLayer/BeamLayer` + HUD |
| **Menu → Game 流程** | ✅ 可跑 | `MenuManager.startGame()` + `BtnStart` |
| **MenuManager** | ✅ 初版 | 开始游戏、选关面板显隐（`BtnLevelSelect`） |
| **GameUIManager** | ✅ 初版 | 返回 Menu、局内选关面板显隐（`BtnBackMenu` / `BtnLevelSelect`） |
| **MusicManager 扩展** | ✅ 初版 | 菜单/局内 BGM、按钮/通关/推动等音效；`PersistRoot` 整树持久化 |
| **Toast.prefab** | ✅ 已修 | `bg` 上 `RoundBox` 脚本引用已对齐本仓 |
| **局内跳关 reload** | ✅ 接口 | `OpticalPuzzleRoot.reloadCurrentLevel()`（列表 UI 未做） |
| Core 推动光学元件 | ✅ 初版 | 单块推、不可拉；连体两块推不动；推后 `_recomputeLighting()` |
| Core 光追 / 胜负 | ✅ 通道模型 | `OpticalBeamTracer` 通道 0～4、格内 L 形折线；入射+元件三步混色；**target 挡光**；**严格同色点亮**；全目标亮通关 |
| **BeamLayer 光路绘制** | ✅ 已接 | 窄线 `Graphics`；支持 RGBW + 黄/青/紫混色输出 |
| **光源/目标占位色** | ✅ 已接 | 光源/目标按 `colorKey` 着色；目标未亮暗色、点亮亮色 |
| **元件通道占位绘制** | ✅ 初版 | `OpticalPuzzlePieceGlyph` 画通道臂；按层 3 `RGBW` 染色 |
| 选关 **列表 UI / 跳关** | ❌ 未做 | 面板显隐已有；关卡项按钮与 `LevelManager` 仍占位 |
| Game 场景 **脚本挂接** | ⚠️ 基本已挂 | `GameUIManager` / `OpticalPuzzleRoot` / `BoardView` / `InputHud` 已挂；`inputHud` 须拖 `InputHub`；HUD 缺 `Button` |
| 传送门 / 多入射汇合混色 | ❌ 未做 | `{`/`}` 已解析；光追未接传送；同格多束光未汇合 |
| 评星 / minSteps | ❌ 已移除 | 关卡文件只做信息存储 |
| 正式美术 / TileMap | ❌ 未做 | 棋盘为 Graphics 色块 + 通道占位 |
| JSON 关卡文件 | ❌ 未做 | 当前为 TS 内四层字符串数组 |

**结论**：处于 **Prototype** — Menu/Game 可进局；**通道光追 + 混色染色 + 推箱 + 目标挡光/同色点亮** 已可玩（关 2 双 L 镜、关 3 通道折光）；**传送门 / 选关 UI / winPanel** 仍为下一优先级。

---

## 3. 关键文件索引

| 路径 | 职责 |
|------|------|
| `assets/Scenes/Menu.scene` | 菜单：`MenuRoot` + `PersistRoot` |
| `assets/Scenes/Game.scene` | 局内：`GameRoot` + 玩法/HUD/Overlay |
| `assets/Scripts/Manager/MenuManager.ts` | Menu 编排：读档、开始游戏、选关面板显隐 |
| `assets/Scripts/Manager/GameUIManager.ts` | Game 编排：回 Menu、局内选关面板显隐 |
| `assets/Scripts/Manager/MusicManager.ts` | 持久化音频：BGM 随场景、音效经 `PLAY_AUDIO` |
| `assets/Scripts/Manager/ToastManager.ts` | 持久化 Toast（`Toast.prefab`） |
| `assets/Scripts/Manager/DataManager.ts` | 单例存档，**不挂节点** |
| `assets/Scripts/Manager/LevelManager.ts` | 关卡列表占位 |
| `assets/Scripts/MenuOverlayWindow.ts` | 弹层关闭 + `RoundBox` 首帧刷新 |
| `assets/Prefab/Toast.prefab` | 子节点约定：`text`（Label）、`bg`（RoundBox） |
| `assets/Scripts/Games/OpticalPuzzle/Config/OpticalPuzzleLevelSchema.ts` | 四层类型、`parseLayeredGridsToLevelConfig`、`overlayLayersFromObjects`、`applyStaticWallsToLayer` |
| `assets/Scripts/Games/OpticalPuzzle/Config/OpticalPuzzleLevels.ts` | 1～6 关四层源数据、`getOpticalLevelById` |
| `assets/Scripts/Games/OpticalPuzzle/Core/OpticalPuzzleCore.ts` | 行走 + **推箱** + 光追重算 + 快照（含 sources/targets/pieces 色） |
| `assets/Scripts/Games/OpticalPuzzle/Core/OpticalBeamTracer.ts` | 通道 0～4 光追；墙/主角/**target**/挡光格(0) 挡光；格内 L 形；混色出射 |
| `assets/Scripts/Games/OpticalPuzzle/Core/OpticalPieceConnectivity.ts` | 通道开口掩码、旋转、`propagationToEntrySide` |
| `assets/Scripts/Games/OpticalPuzzle/Core/OpticalColorMix.ts` | 三步混色（分解累加 → R/G/B 均≥1 减最小 → 两色合成黄/青/紫） |
| `assets/Scripts/Games/OpticalPuzzle/Core/OpticalLightColor.ts` | `colorKey`、混色键解析、**严格同色** target 判定 |
| `assets/Scripts/Games/OpticalPuzzle/Core/OpticalMirrorReflection.ts` | **遗留**（旧斜镜表；光追已改用通道模型） |
| `assets/Scripts/Games/OpticalPuzzle/Presentation/OpticalPuzzleRoot.ts` | 局内入口；`BoardView` + `BeamView`（可自动挂 `BeamLayer`） |
| `assets/Scripts/Games/OpticalPuzzle/Presentation/OpticalPuzzleBoardView.ts` | 棋盘 Graphics；光源/目标/玩家；**元件通道占位 + 染色** |
| `assets/Scripts/Games/OpticalPuzzle/Presentation/OpticalPuzzlePieceGlyph.ts` | 通道臂 `Graphics` 绘制（格心留空） |
| `assets/Scripts/Games/OpticalPuzzle/Presentation/OpticalPuzzleBeamView.ts` | 光路窄线 Graphics（含混色） |
| `assets/Scripts/Games/OpticalPuzzle/Presentation/OpticalPuzzleInputHud.ts` | 四向 / 撤回 / 重置 |
| `assets/Scripts/Utils/Enum.ts` | `SCENE_ENUM`、`AUDIO_EFFECT_ENUM`、`BGM_KIND_ENUM` 等 |

---

## 4. 关卡数据约定（当前实现）

### 4.1 源数据形状（四层同尺寸）

```typescript
// IOpticalLevelLayeredSource（四层均为必填）
{
  levelId: number;
  levelName: string;
  height: number;
  width: number;
  staticLayout: string[];  // 层1
  objects: string[];       // 层2
  colors: string[];        // 层3
  directions: string[];    // 层4
}
```

运行时经 `parseLayeredGridsToLevelConfig(src)` → `IOpticalLevelConfig`（`terrain`、`player`、`pieces`、`sources`、`targets`）。  
**已废弃**：单表 `grid` + `type×color` 单字符编码（旧 `o/r/g/b`… 表）。

编关辅助（正式数据仍须写齐四层）：`overlayLayersFromObjects` 生成稀疏层 3/4 草稿 → `applyStaticWallsToLayer(staticLayout, layer)` 对齐内部墙 `#` → 核对每行 `length === width` 后写入 `OpticalPuzzleLevels.ts`。

### 4.2 分层字符表（与 `OpticalPuzzleLevelSchema.ts` 一致）

| 层 | 用途 | 主要字符 |
|----|------|----------|
| **1 staticLayout** | 静态墙/石块（合并为 `#`） | `#` `.` |
| **2 objects** | 玩家、光源、目标、元件几何 | `#` `.` `@` `S` `E` **`0`～`4`**（通道）；旧 `/` `\|` `T` `+` `{` `}` 兼容 |
| **3 colors** | 光色 **RGBW**（仅 S/E/元件格） | `#` 墙；空地/玩家 `.`；`W` 透传默认，`R`/`G`/`B` 元件染色/目标期望色；物件格 `.` 解析为 `W` |
| **4 directions** | 朝向（仅需要者） | `#` 墙；空地/玩家/目标 `.`；`S` 等写 `wasd`；未写 `.` 时光源默认朝下 |

**硬性约定**：

- 每关 **四层数组缺一不可**（`OpticalPuzzleLevels.ts` 内全部显式配置）。
- `staticLayout` 为 `#` 的格 → **四层均为 `#`**（含内部柱/十字墙）。
- 层 2 元件推荐写 **`0`～`4`**（见 `OpticalPuzzleLevelSchema.ts` 通道图）；层 4 `w/a/s/d` 旋转；`0` 挡光，`1` L 弯，`2` 直通，`3` T，`4` 十字。
- 层 3/4 **不要**在空地铺 `W`；只在有 `S`/`E`/元件的格写颜色/方向。
- 混色规则见 `OpticalColorMix.ts`：入射光 + 元件层 3 色共同参与第一步累加；第二步 R/G/B **均 ≥1** 时各减最小值。
- 每层每行字符串长度 **必须等于 `width`**（多/少一个字符即解析失败）。

### 4.3 已有示例关（`OpticalPuzzleLevels.ts`）

| levelId | levelName | 内容概要 |
|---------|-----------|----------|
| 1 | 教学 | 7×7 直线：`S`↓ `@` `E`（白光） |
| 2 | 绕十字 | 9×9 绕十字；顶行 `S` `@` `E`；两行 **`1`** L 通道镜（`d`/`w` 朝向） |
| 3 | 柱廊 | 11×7 柱廊；`S` `@` `E` 同行；**`1`** L 通道 + 中间 **`1`** 折光 |
| 4 | 穿门洞 | 13×9 竖墙门洞，`S` 上 `@` 中 `E` 下 |
| 5 | 光源与目标 | 11×9 大房间 `S` `@` `E` |
| 6 | 双目标 | 10×8 `S` + 两个 `E` |

未知 `levelId` 时 `OpticalPuzzleRoot` 回退 `DEV_LEVEL_MINIMAL`（levelId 0）。

---

## 5. 运行时行为（Agent 须知）

### 5.1 Core / 棋盘 / 光追

- **Core 行走**：`tryMove` 可走地板；**不可走进** `S`/`E` 格。
- **Core 推箱**：正前方有元件且再前方为空地板时可推 **一块**；连续两格都有元件则推不动；推动成功 `MoveAttemptResult.PiecePushed`。
- **撤回**：`OpticalPuzzleSession` 历史栈保存 `player` + `pieces` 坐标；`undo` 后恢复并重算光路。
- **光追模型**：统一 **通道 0～4**（非旧斜镜反射表）；`connectivity===0` 或墙/主角/**target** 挡光；有通道的元件格内画 **L 形折线**（入射边→格心→出射边）。
- **混色**：光穿过元件时 `mixLightColors([入射色, 元件层3色])`；支持黄/青/紫二次色；`W` 元件参与累加但不改单色结果（白+白→白）。
- **target 规则**：光线在 target **入口边界面停止**（不穿透）；仅 `lightMatchesTarget` **严格同色**（含混色键）时点亮；`allTargetsLit` → Session `SETTLEMENT` + `OPTICAL_LEVEL_COMPLETE`。
- **BoardView**：墙/地板/光源/目标/玩家；**元件**用 `drawConnectivityGlyph` + `pieceChannelColors` 按 `RGBW` 染色。
- **BeamView**：`BeamLayer` 窄线，颜色随段 `colorKey`（含混色）。
- **首帧棋盘**：`BoardView.render` 内 `_ensureGraphics()`（无需按 R 才显示）。
- **存档**：`opticalCurrentLevelId` 为 **number**；旧字符串 id 读档映射到 0～6。
- **局内跳关**：改 `DataManager.opticalCurrentLevelId` 后调 `OpticalPuzzleRoot.reloadCurrentLevel()`。

### 5.2 场景节点与 Manager 挂接（已定稿）

**PersistRoot（仅 Menu 场景建一次，切场景保留）**

```
PersistRoot
├── AudioService    MusicManager + AudioSource
└── ToastService    ToastManager + Toast.prefab
```

`MusicManager` 对 **整棵 `PersistRoot`** 执行 `addPersistRootNode`。**Game 场景勿重复建**。

**Menu.scene**

```
Canvas → MenuRoot（MenuManager）
  layerBackground / layerMain / layerOverlay
MainPanel：BtnStart、BtnLevelSelect → 拖入 MenuManager
LevelSelectPanel → MenuOverlayWindow；默认 inactive
```

**Game.scene**（根节点名须为 **`Game`**，与 `SCENE_ENUM.GAME` 一致，否则局内 BGM 不切换）

```
Canvas → GameRoot（GameUIManager）
  layerBackground → Background
  layerPlay → OpticalPuzzle（OpticalPuzzleRoot）
    BoardLayer（OpticalPuzzleBoardView）
    BeamLayer（光路预留）
  layerHud → TopBar（BtnBackMenu、BtnLevelSelect、LabelLevel）
           → InputHub（OpticalPuzzleInputHud + 四向/撤回/重置）
  layerOverlay → LevelSelectPanel、WinPanel 等（默认 inactive）
```

**Manager 分工**

| 脚本 | 挂哪 | Game 要挂吗 |
|------|------|-------------|
| `DataManager` | 不挂节点，代码单例 | — |
| `MusicManager` / `ToastManager` | Menu `PersistRoot` | 否 |
| `MenuManager` | Menu `MenuRoot` | 否 |
| `GameUIManager` | Game `GameRoot` | 是 |
| `OpticalPuzzleRoot` | `OpticalPuzzle` | 是 |
| `OpticalPuzzleInputHud` | `InputHub` | 是 |
| `LevelManager` | 选关列表做好后再挂 | 可选 |

**按钮绑定原则**：按钮留在 `TopBar` / `InputHub` / 弹层下，**用 `@property` 拖引用**；`GameUIManager` 只管流程类按钮（返回、开选关），四向/撤回给 `InputHud`，关卡列表项给未来选关 Panel。**勿在 Button Click Events 与脚本重复绑同一逻辑**。

### 5.3 音频（MusicManager）

| 属性 | 用途 |
|------|------|
| `bgmMenu` / `bgmGame` | 场景 `Menu` / `Game` 自动切换 BGM |
| `clickButton` | UI 点击 |
| `opticalLevelComplete` | 通关成功 |
| `opticalPiecePushSuccess` / `opticalPiecePushFail` | 推动成功 / 失败 |
| `DataManager.bgmOn` / `sfxOn` | 设置页开关（读存档） |

音效经 `PLAY_AUDIO.emit(EVENT_ENUM.PLAY_AUDIO, AUDIO_EFFECT_ENUM.*)`。

### 5.4 数据层与 UI 层（架构约定）

- **Core**：维护地形表、占用/元件表、光路表；只输出**只读 Snapshot**。
- **Application**：`OpticalPuzzleSession` 驱动变更与历史栈。
- **Presentation**：`BoardLayer` 读棋盘快照；`BeamLayer` 读光路快照；**不写规则**。
- **关卡尺寸适配**：缩放 **`OpticalPuzzle` 节点 scale**（Board + Beam 同父），**不用 Camera** 缩放，以免 HUD 一起变。

---

## 6. 本轮对话沉淀

### 2026-05-23（关卡数据）

1. **关卡文件形态迭代**：`height` + `width` + `grid` 字符行；去掉 `minSteps`、评星步数。
2. **levelId 整数 + levelName**；全表字符解析；目标 `E`；传送门 `{`/`}`。
3. **讨论未落地**：JSON 关卡、评星步数等。

### 2026-05-24（四层关卡数据 v2 + 局内地图）

**关卡数据（设计 → 实现）**

1. **四层网格取代单表 `grid`**：`staticLayout`（静态墙/石块 `#`）/ `objects`（`@` `S` `E` + 元件几何）/ `colors`（**RGBW**）/ `directions`（`wasd`）。
2. **光色 RGBW**：`W` 白光默认；`R`/`G`/`B` 红绿蓝；层 3 仅在光源/目标/元件格标注，空地与玩家为 `.`（未写按 `W` 解析仅针对物件格）。
3. **解析器**：`parseLayeredGridsToLevelConfig`；辅助 `overlayLayersFromObjects`、`applyStaticWallsToLayer`、`blankOverlayLayersFromStatic`。
4. **示例关 1～6**：全部手写四层；2～4 关补全 `S` `@` `E` 可走动线；废弃旧「类型×颜色单字符」表。
5. **文档同步**：`小游戏设计文档.md` §5（v1.1）、`框架搭建说明.md` §5、`OpticalPuzzleLevelSchema.ts` 文件头注释。

**局内表现 / 编辑器**

6. **`OpticalPuzzleBoardView`**：`render` 前 `_ensureGraphics()`，解决进 Game 棋盘空白、按 R 才刷出的问题。
7. **Game 场景挂接**：`OpticalPuzzleRoot`（`OpticalPuzzle`）、`BoardView`（`BoardLayer`）、`InputHud`（`InputHub`）；`boardView` 可自动找子节点，`inputHud` 须手动拖 `InputHub`（与 `OpticalPuzzle` 非父子）。
8. **预览排错**：关 2 `directions` 内部墙未标 `#`；关 3/4 `colors`/`directions` 行宽多 1 字符 → 已按 `width` 修正。

**未做（仍属下一迭代）**

- `BoardView` 绘制 `pieces`；`BeamLayer` 光路。
- Core 读 `pieces`、推动、光追与胜负。
- Game HUD 节点补 `Button` 组件；`OpticalPuzzle` scale 关卡适配。

### 2026-05-25（光追 + BeamLayer + 通关）

1. **`OpticalBeamTracer`**：多光源直线光追；墙/主角/元件挡光；严格同色点亮目标。
2. **`OpticalPuzzleCore`**：移动/重置/撤回后重算；快照含 `sources`/`targets`/`allTargetsLit`；`getBeamSnapshot()`。
3. **`OpticalPuzzleBeamView`**：`BeamLayer` 窄线绘制，颜色 RGBW 与光源一致。
4. **`OpticalPuzzleBoardView`**：光源白色（或彩色）方块；目标未亮 `#262a36` 系、点亮按色提亮。
5. **`OpticalPuzzleSession`**：全目标亮 → `SETTLEMENT` + `complete` 通知；`OPTICAL_LEVEL_COMPLETE` 音效。
6. **`OpticalPuzzleRoot`**：同步刷新棋盘与光路；`BeamLayer` 无脚本时自动挂载 `OpticalPuzzleBeamView`。

**未做**：镜子/分光/滤光/传送；元件推动；`winPanel` UI。

### 2026-05-25（通道光追 + 混色 + 推箱 + target 规则）

1. **统一通道 0～4**：层 2 数字编码 + 层 4 旋转；`OpticalPieceConnectivity` + `OpticalPuzzlePieceGlyph` 占位绘制；弃用 `OpticalMirrorReflection` 于光追主路径。
2. **三步混色** `OpticalColorMix`：分解累加 → R/G/B **均≥1** 减最小值 → 两色合成黄/青/紫；光过元件时入射色与元件层 3 色累加（例：白+红→红）。
3. **元件染色**：快照带 `colorKey`；`BoardView` / `PieceGlyph` 按 `RGBW` 画通道臂。
4. **推箱** `OpticalPuzzleCore.tryMove`：单块推、连体不推；撤回栈含 `pieces` 坐标。
5. **target 挡光 + 严格同色点亮**：光在 target 边界面停止、不穿透；`lightMatchesTarget` 比较完整 `colorKey`（含混色）。
6. **关卡**：关 2 改为双 `1` L 通道；关 3 加通道折光元件。

**未做**：传送门光路；同格多束光汇合混色；`winPanel`；选关列表 UI；混色目标与二次色通关判定细则。

### 2026-05-23（Menu / Game 场景与流程）

1. **Menu 节点框架**：`MenuRoot` 三分层 + 场景根级 `PersistRoot`（Audio/Toast 同层）。
2. **Game 节点框架**：`GameRoot` 四层；`OpticalPuzzle` 下 `BoardLayer` + `BeamLayer`。
3. **MenuManager**：`BtnStart` → `loadScene(Game)`；`BtnLevelSelect` + `LevelSelectPanel` 显隐。
4. **GameUIManager**：`BtnBackMenu` → `loadScene(Menu)`；局内 `BtnLevelSelect` 开选关面板。
5. **MusicManager**：菜单/局内双 BGM + 多种音效枚举；PersistRoot 整树持久化。
6. **Toast.prefab 修复**：`bg` 上错误 `RoundBox` 脚本 id（旧项目拷贝）→ 本仓 `RoundBox`。
7. **OpticalPuzzleRoot.reloadCurrentLevel()**：供局内跳关重载（列表 UI 待做）。
8. **架构共识**：数据驱动 UI；不同尺寸关卡用 `OpticalPuzzle` scale，非 Camera。

---

## 7. 建议下一步（优先级）

1. **传送门**：`{`/`}` 解析已有；光追跨格跳转 + 环检测。
2. **多入射汇合**：T(3)/十字(4) 同格多方向入射时的队列汇合与 `mixLightColors` 多色输入。
3. **Presentation**：`winPanel` 通关 UI；`OpticalPuzzle` scale 关卡适配。
4. **编辑器收尾**：`InputHud` 拖引用；HUD/TopBar 补 `Button`；选关列表 → `reloadCurrentLevel()`。
5. **关卡内容**：滤光教学关（层 3 `R`/`G`/`B` + 对应色 `E`）；分光 `3`/`4` 专项关。
6. **规则收口**：混色目标（黄/青/紫 `E`）是否计入通关；`applyPieceColorMode` 遗留函数清理或文档对齐。
7. **每轮结束**：更新本文档 §2、§6、§7。

---

## 8. Agent 维护说明

- 完成可验证功能后，更新 **§2 总体进度** 与 **§6 本轮变更**（追加日期段落，勿删历史）。
- 四层字符表变更必须 **同时改** `OpticalPuzzleLevelSchema.ts` 注释、解析逻辑、`OpticalPuzzleLevels.ts` 示例与本文 §4.2。
- 新增关卡后建议在编辑器预览一次，避免 `行数 ≠ width` / `静态墙四层须均为 #` 类解析错误。
- 新增关卡只改 `OpticalPuzzleLevels.ts`（或未来 JSON），保持 `levelId` 连续或文档说明跳号。
- 不要在不询问用户的情况下恢复已删除的 `minSteps` / 评星字段，除非需求变更。
- 场景/Managers 约定以 **§5.2** 为准；勿在 Game 重复创建 `PersistRoot`。

---

## 9. 相关文档

- 玩法与规则全文：`小游戏设计文档.md`
- 60 天排期：`项目开发排期.md`
- 场景挂接：`框架搭建说明.md`
- 工程规范：`.cursor/rules/project-rules.mdc`
